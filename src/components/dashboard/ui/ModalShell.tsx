"use client";

import { Modal } from "@/components/ui/Modal";

/**
 * ModalShell — branded dialog shell for dashboard pages.
 * Thin alias over the shared Modal (animated, gradient header, footer slot)
 * so kit consumers have one import path.
 */
export const ModalShell = Modal;
